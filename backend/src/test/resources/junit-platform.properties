# This file takes precedence over junit-platform.properties bundled inside
# kafka-clients, kafka-server-common and kafka_2.13 test JARs, preventing
# their settings (e.g. parallel execution, auto-detected extensions) from
# interfering with our test suite.
junit.jupiter.extensions.autodetection.enabled=false
junit.jupiter.execution.parallel.enabled=false
